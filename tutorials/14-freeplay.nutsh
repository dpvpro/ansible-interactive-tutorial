lesson_name("Free play")
make_and_go_ws
clear_ws
run(`cp -r /tutorials/files/ansible-vertica/* .`)

"This is not a lesson, rather an option to let you play with ansible!"

"Remember you can quit by typing `exit` or `done` once you're done!"

prompt {
    if command == "done" {
        expect ("done")
        break
    }
}
